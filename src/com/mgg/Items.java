package com.mgg;

public class Items {

	//Make three different classes for the different types of input. When tokenizing
	//the inputs from the file, make an if statement that says, if token[n] == "String"
	//then create class from specified type
	//
	//Make one class under Items, and name the 4th token from the file something 
	//generic
}
